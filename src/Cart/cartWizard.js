import React from 'react';
import {Header} from '../header';

export const CartWizard = (props) => {
    return (
        <div className="cart container body-content">
            <Header />
                Cart
        </div>
    );
}
