import React from 'react';
import {Header} from '../header';

export const RegistrationWizard = (props) => {
    return (
        <div className="registration container body-content">
            <Header />
                Registration
        </div>
    );
}
